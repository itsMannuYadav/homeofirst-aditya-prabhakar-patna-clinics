# Groq API key for My First Care chatbot (server-only — never expose to the client)
# Get a free key at https://console.groq.com/keys
# Copy this file to .env.local and set your real key there (never commit real keys)
GROQ_API_KEY=
